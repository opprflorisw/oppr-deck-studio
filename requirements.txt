# Oppr Deck Studio — Python dependencies for the tooling.
#   pip install -r requirements.txt
# Also required (not pip): Google Chrome or Microsoft Edge, for HTML -> PDF/PNG
# rendering (tools/build-pdf.ps1 and the catalog/design-system renderers).

PyYAML>=6.0        # deck.yaml / meta.yaml / recipe parsing
pypdf>=4.0         # PDF page-count / mediabox checks
PyMuPDF>=1.24      # imported as 'fitz' — PDF -> PNG rasterization (verify, catalog)
Pillow>=10.0       # thumbnail / image inspection
