# assemble the spec

`wayfinder:grilling` · child of `../MAP.md` · unassigned

**Blocked by:** `001` · `002` · `003` · `004` · `005` · `006`

## Question

Assemble the resolved decisions into the written spec for the "Welcome a colleague"
output type.

The destination ticket. Once the others are closed, write the spec (likely a new
`knowledge/best-practices/welcome-colleague.md` plus a note in the app IA doc)
covering, in order: what the output is, the upload + consent flow, the cartoon
generation recipe, the image template, the post template, where it lives in the
app, the build and verify rules, and the likeness/consent policy. It should read
like the carousel playbook: enough that someone could build it without re-deciding
anything.

Blocked by: all other tickets.
