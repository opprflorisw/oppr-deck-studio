# Tab identity and icons

**Type:** grilling · **Blocked by:** — (quick win, but final IA depends on app-role) · **Status:** open

## Question

Give every area's tabs a clear identity — starting with your explicit ask: icons
in front of the **Output** tabs (Decks, Social output), and by extension every
area's tabs (Library: Slides/Graphics/Icons/Design system; Knowledge: the four
docs).

To settle:
- Which icon represents each tab (reuse the app icon set; add any missing).
- Whether counts/badges sit on tabs (e.g. Social output posted/archived counts).
- Active-tab styling and whether the area title stays alongside the tabs.

Note: the *set* of tabs may change once *App role* and *Fate of the in-app
builder* resolve (Create's tabs may disappear) — so lock the pattern here, apply
the final tab list after those.
